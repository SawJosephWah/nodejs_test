let originalString = "immutable";
let modifiedString = originalString.replace("immutable", "mutable");

console.log(originalString); // Outputs: "immutable"
console.log(modifiedString); // Outputs: "mutable"